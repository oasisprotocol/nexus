// Package healthcheck implements the `healthcheck` sub-command. This is intended
// to serve as a status check of indexer progress, to validate that the
// current status of the indexer is as expected.
package healthcheck
